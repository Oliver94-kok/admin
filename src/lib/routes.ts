export const PublicRoute = ["/", "/signup"];

export const protectedRoutes = ["/dashboard"];

export const apiAuthPrefix = [
  "/api/auth",
  "/api/user",
  "/api/users/signin",
  "/api/attend",
];

export const Default_login_Redirect = "/";
